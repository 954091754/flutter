import 'package:flutter/material.dart';

class VarietyPage extends StatelessWidget {
  const VarietyPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('综艺 页面', style: TextStyle(fontSize: 18)));
  }
}
